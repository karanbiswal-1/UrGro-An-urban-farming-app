<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.urgro">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".changePassword"></activity>
        <activity android:name=".happyfarming" />
        <activity android:name=".urbanShow" />
        <activity android:name=".chatShow"></activity>
        <activity android:name=".exploreshow" />
        <activity
            android:name=".forgot_password"
            android:parentActivityName=".loginActivity" />
        <activity android:name=".myPostActivity" />
        <activity android:name=".Adapter.previewPost" />
        <activity android:name=".messageActivity" />
        <activity android:name=".accountActivity" />
        <activity android:name=".editProfileActivity" />
        <activity android:name=".searchActivity" />
        <activity android:name=".adPostActivity" />
        <activity android:name=".dashBoard" />
        <activity android:name=".registerActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>